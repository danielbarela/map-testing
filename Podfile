source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '13.0'

workspace 'MAGE'
project 'MAGE.xcodeproj'

use_frameworks!

target 'MAGE' do
    target 'MAGETests' do
      inherit! :search_paths
      pod 'Quick'
      pod 'Nimble', '~> 9'
    end
end
